spring:
  application:
    name: restaurant-finder
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurants
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
  data:
    mongodb:
      host: localhost
      port: 27017
      database: restaurant_menus
      username: admin
      password: admin123
      authentication-database: admin

server:
  port: 8080

logging:
  level:
    com.learn.restaurants: DEBUG
